namespace Esatto.Umbraco.Backoffice.CrossContent;

/// <summary>A content type surfaced to the picker's type dropdown (label resolved).</summary>
public sealed record CrossContentContentTypeInfo(string Alias, string Label);
